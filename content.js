(() => { let box; const esc=s=>String(s??"").replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c])); function siteDelay(){const nav=performance.getEntriesByType('navigation')[0];return nav&&nav.responseEnd>0?Math.round(nav.responseEnd-nav.startTime):null;} function nodePlace(country,url,ip){const n=country||"未知";const image=url?`<img class="pch-flag-img" src="${url}" alt="${esc(n)}" onerror="this.style.display='none'">`:'';return `<span class="pch-place">${image}<span>${esc(n)}</span>${ip?`<small>${esc(ip)}</small>`:''}</span>`;} function render(data){if(!data||data.error)return;if(!box){box=document.createElement('div');box.id='proxy-chain-helper';document.documentElement.appendChild(box)}const direct=!data.found||data.node==='DIRECT';const nodeCountry=nodePlace(data.nodeCountry,data.nodeFlagDataUrl,data.nodeIp);const nodeDelay=data.delay==null?'节点：—':`节点：${data.delay} ms`;box.innerHTML=`<span class="pch-flag">${direct?'↔️':(data.nodeFlagDataUrl?`<img class="pch-flag-img" src="${data.nodeFlagDataUrl}" alt="${esc(data.nodeCountry||'未知')}" onerror="this.style.display='none'>`:'🌐')}</span><span class="pch-main"><strong>${direct?'直连':esc(data.chainLabel||data.node||'未知')}</strong><small>落地节点：${nodeCountry} · ${nodeDelay}</small></span><button title="隐藏">×</button>`;box.style.left='';box.style.right='';box.style.bottom='';box.style[window.__pchSide||'right']='14px';box.style.bottom='14px';box.querySelector('button').onclick=()=>box.classList.add('pch-hidden');box.onmouseenter=()=>box.classList.remove('pch-hidden');}chrome.runtime.onMessage.addListener((m,_s,sendResponse)=>{if(m.type==='overlayResult')render(m.data);if(m.type==='getSiteDelay'){sendResponse({siteDelay:siteDelay()});return true;}});chrome.runtime.sendMessage({type:'overlayConfig'}).then(c=>{if(!c?.overlayEnabled)return;window.__pchSide=c.overlaySide||'right';chrome.runtime.sendMessage({type:'inspect',target:location.hostname,siteDelay:siteDelay()}).then(render).catch(()=>{});}).catch(()=>{});})();